function spiral2DArray(arr) {
    let m = arr[0].length;
    let n = arr.length;
    let total = m * n;
    let index = 0;

    while (index < total) {}
}

let output = spiral2DArray([
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
]);

console.log(output);
